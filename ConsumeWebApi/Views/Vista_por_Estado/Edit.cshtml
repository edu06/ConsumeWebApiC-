@model ConsumeWebApi.Models.Vista_por_Estado

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Vista_por_Estado</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Estado)

        @Html.HiddenFor(model => model.Usuario_Tecnico)

        @Html.HiddenFor(model => model.Codigo_Incidencia)

        @Html.HiddenFor(model => model.Titulo_Incidencia)

        @Html.HiddenFor(model => model.Descripcion)

        @Html.HiddenFor(model => model.Nombre_Cliente)

        @Html.HiddenFor(model => model.Apellido_Cliente)

        @Html.HiddenFor(model => model.Nombre_Receptor)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
