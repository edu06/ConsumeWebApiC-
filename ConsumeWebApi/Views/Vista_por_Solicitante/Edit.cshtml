@model ConsumeWebApi.Models.Vista_por_Solicitante

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Vista_por_Solicitante</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Nombre_Solicitante)

        @Html.HiddenFor(model => model.Apellido_Solicitante)

        @Html.HiddenFor(model => model.Usuario_tecnico)

        @Html.HiddenFor(model => model.Codigo_Incidencia)

        @Html.HiddenFor(model => model.Titulo_Incidencia)

        @Html.HiddenFor(model => model.Descripcion_Incidencia)

        @Html.HiddenFor(model => model.Nombre_Receptor)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
